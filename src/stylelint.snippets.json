{
  "stylelint disable line": {
    "prefix": "stylelint-disable-line",
    "body": ["/* stylelint-disable-line ${0:rule} */"],
    "description":
      "Disable Stylelint on this line. If no rule is specified, then all rules will be disabled."
  },
  "stylelint-disable-next-line": {
    "prefix": "stylelint-disable-next-line",
    "body": ["/* stylelint-disable-next-line ${0:rule} */"],
    "description":
      "Disable Stylelint on the next line. If no rule is specified, then all rules will be disabled."
  },
  "stylelint-disable-block": {
    "prefix": "stylelint-disable-block",
    "body": [
      "/* stylelint-disable ${1:rule} */",
      "$0/* stylelint-enable ${1:rule} */"
    ],
    "description":
      "Disable Stylelint in a block of code. If no rule is specified, then all rules will be disabled."
  }
}